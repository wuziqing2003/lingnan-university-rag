# RAG 优化实验报告

> 实验范围：高校规章 RAG 系统的切块策略、混合检索与 Rerank 精排对照  
> 语料基础：岭南师范学院教务相关规章 PDF（当前约 67 份，1474 个文本块）

---

## 实验一：Chunking 策略对照

### 1. 实验目标

高校规章通常包含章、条、款和列举项。若直接按固定字符数切块，可能把一条完整规定拆成上下两半，导致后续检索只召回前提或结论之一。

本实验比较以下三种切块方式：

1. **Naive 硬切**：使用 `text[i:i+chunk_size]` 固定长度切片。
2. **CharacterTextSplitter**：设置 `separator=""`，作为 LangChain 固定字符切块基线。
3. **RecursiveCharacterTextSplitter**：依次尝试段落、换行、句号、分号、逗号等边界。

实验主要回答两个问题：

- 固定长度切块是否容易造成句中腰斩？
- `128 / 256 / 512` 中，哪一个尺寸更适合作为学校规章的默认参数？

### 2. 实验设置

| 项目 | 设置 |
|------|------|
| 样本文档 | 《岭南师范学院普通全日制本科生学籍管理规定（2025 年修订）》 |
| 样本范围 | 第一条至第九条 |
| 样本长度 | 1563 字符 |
| Chunk Size | 128、256、512 |
| Chunk Overlap | 0（控制变量，避免重叠掩盖边界问题） |
| 长度计算 | Python `len`，按字符计数 |
| Recursive 分隔符 | `["\n\n", "\n", "。", "；", "，", " ", ""]` |
| 实验脚本 | `playground/chunk_test.py` |
| 固定样本 | `data/chunk_experiment_sample.txt` |

选择该样本的原因：

- 条款编号和段落结构清晰；
- 包含较长句子和多项列举；
- 第九条包含新生复查事项，适合观察条款是否被拆散。

### 3. 结果

#### 3.1 客观统计

| 方法 | Size | 块数 | 最短块 | 最长块 | 平均长度 | 以句末标点结束 | 小于 64 字的块 |
|------|-----:|-----:|-------:|-------:|---------:|-----------------:|----------------:|
| Naive | 128 | 13 | 27 | 128 | 120.0 | 1 | 1 |
| Character | 128 | 13 | 27 | 128 | 120.0 | 1 | 1 |
| Recursive | 128 | 14 | 93 | 126 | 110.6 | 6 | 0 |
| Naive | 256 | 7 | 27 | 256 | 223.0 | 1 | 1 |
| Character | 256 | 7 | 27 | 256 | 223.0 | 1 | 1 |
| Recursive | 256 | 8 | 35 | 254 | 194.4 | 1 | 2 |
| Naive | 512 | 4 | 27 | 512 | 390.5 | 1 | 1 |
| Character | 512 | 4 | 27 | 512 | 390.5 | 1 | 1 |
| Recursive | 512 | 4 | 44 | 512 | 389.8 | 1 | 1 |

说明：

- “以句末标点结束”仅统计 `。；！？`，是辅助指标，不等同于语义完整率。
- Recursive 也可能在换行边界切开，因此不能只靠句末标点数量判断优劣。
- 本实验没有使用 overlap，生产参数中的 overlap 需另行验证。

#### 3.2 主要观察

**Naive 与 Character 基本等价。** Character 设置 `separator=""` 后，其块数、长度和边界与手写固定切片一致。它在本实验中不是第三种新策略，而是 LangChain 对固定字符切块的标准实现。

**固定长度切块存在明显腰斩风险。** Naive 和 Character 会在达到长度上限时直接截断，不关心当前位置是否位于句子、条款或列举项中间。增大 size 只能减少切块次数，不能保证边界自然。

**Recursive 更倾向于利用文档结构。** Recursive 会按分隔符优先级寻找边界，只有找不到合适边界时才退化为硬切。它在 128 字实验中产生了更多以句末标点结束的块，说明结构感知切块确实能够改善部分边界。

**Recursive 仍可能产生短残块。** 在 256 字实验中，Recursive 产生了 35 字和 44 字等较短块。这说明仅依赖递归分隔并不能保证块大小完全均衡。

**PDF 抽取换行会影响自动判定。** 原文中的“复查”被 PDF 抽取为 `复\n查`，导致基于完整字符串的金标准匹配失败。这属于文本抽取和评估规则问题，不代表切块过程丢失了内容。

### 4. 参数结论

当前项目采用以下生产参数：

```text
splitter = RecursiveCharacterTextSplitter
chunk_size = 256
chunk_overlap = 50
separators = ["\n\n", "\n", "。", "；", "，", " ", ""]
length_function = len
```

选择 `256` 而不是 `128` 或 `512` 的原因：

- **128**：检索粒度细，但长条款更容易被拆成多个块，上下文不足风险较高。
- **256**：在条款完整性、块数量和检索粒度之间更平衡。
- **512**：上下文更完整，但可能把多个不同条款混入同一块，增加召回噪声。

`chunk_overlap=50` 未参与本次受控实验，作为生产候选值用于降低边界信息丢失风险。

### 5. 对项目的影响

实验结论已落地到 `scripts/ingest_pdfs.py`：

- 固定长度切片替换为 `RecursiveCharacterTextSplitter`；
- 默认 `chunk_size` 调整为 256；
- 默认 `chunk_overlap` 设置为 50；
- Chroma 集合已重新入库，共处理 67 份 PDF，生成 1474 个 chunk。

### 6. 实验局限

本实验只证明了切块边界的结构差异，尚不能直接证明最终问答准确率提升。

当前局限包括：

1. 仅使用一份文档中的 1563 字样本；
2. 边界质量以人工观察和简单启发式指标为主；
3. 没有比较 overlap 为 0 与 50 的检索效果；
4. 没有测量 Hit@3、Context Precision 或最终回答质量；
5. PDF 抽取产生的断行会干扰切块和金标准匹配。

因此，本部分结论限定在切块边界质量与参数选择，不将其直接表述为最终问答准确率的提升。

---

## 实验二：混合检索（Hybrid Search）

### 1. 实验目标

Chroma 原生提供稠密向量检索，但不提供传统 BM25 关键字检索。高校规章问答中同时存在两类问题：

- **语义类**：如“奖学金和助学金有什么区别”；
- **专名 / 精确词类**：如岗位名称、条款主题词。

本实验在不修改 Chroma 内核的前提下，引入轻量级 `rank_bm25`，与向量检索并行，再通过 RRF（Reciprocal Rank Fusion，互惠倒数排名）融合两路结果，验证混合检索是否优于纯向量检索。

实验主要回答三个问题：

1. 能否在现有 Chroma 向量库之上稳定接入 BM25 + RRF？
2. 在真实校规章语料上，Hybrid 与 Dense 的 Top3 表现有何差异？
3. 中文分词方式（正则 vs jieba）对 BM25 通道有何影响？

### 2. 实验设置

| 项目 | 设置 |
|------|------|
| 向量库 | Chroma `lingnan_rag_pdfs` |
| 语料规模 | 约 67 份 PDF / 1474 chunks（Recursive 切块后入库） |
| Embedding | `BAAI/bge-large-zh-v1.5`（硅基流动） |
| 向量通道 | Chroma `query_embeddings`，候选数 `CANDIDATE_N = 10` |
| 关键字通道 | `rank_bm25.BM25Okapi`，候选数 10 |
| 融合算法 | RRF，`k = 60` |
| 最终返回 | Top 3 文本块 |
| 中文分词 | 初版：正则；定稿：`jieba` + 领域词表 |
| 对照脚本 | `scripts/retrieve_probe.py` |
| 实现文件 | `app/rag/hybrid.py` |

**验收问句：**

| 类型 | 问句 |
|------|------|
| 专名 / 流程 | 研究生三助一辅岗位怎么申请？ |
| 语义区分 | 研究生国家奖学金和国家助学金有什么区别？ |

**方法说明：**

```text
问题
  ├─ Dense：问题 Embedding → Chroma Top-10 → id 排名
  ├─ BM25：jieba 分词 → 全库打分 → Top-10 → id 排名
  └─ RRF：对两路 id 累加 1/(k+rank) → 取 Top-3 正文
```

两路结果通过文档 id 对齐。id 在入库阶段按 `文件名_序号` 写入；BM25 索引通过 `collection.get(include=["documents"])` 加载全库文本后在内存构建。

### 3. 结果

#### 3.1 分词修正前后的差异

初版使用正则：

```python
re.findall(r"[\u4e00-\u9fff]+|[a-zA-Z0-9]+", text)
```

该写法倾向于把连续汉字整段视为单个 token，导致 BM25 词项过粗，关键字通道不稳定，Hybrid Top3 中曾出现与问题无关的文档（例如实践教学类规定）被抬入候选。

定稿改为 jieba，并对以下专名执行 `add_word`：

- 三助一辅
- 国家奖学金
- 国家助学金
- 学业奖学金
- 保留入学资格
- 学业预警
- 勤工助学

修正后，问题“研究生三助一辅岗位怎么申请？”的分词结果包含完整专名“三助一辅”，BM25 通道恢复可用。后续对照均基于 jieba 版本。

#### 3.2 Dense 与 Hybrid 对照（jieba 定稿）

**问句 A：研究生三助一辅岗位怎么申请？**

| 通道 | Top1 | Top2 | Top3 |
|------|------|------|------|
| Dense | 《三助一辅岗位管理办法》申请条件 / 不得受聘相关段落 | 同文件：岗位职责说明 + 申请基本条件 | 同文件：岗位管理与考核章节 |
| Hybrid | 同 Dense Top1（条件 / 不得受聘） | 同文件：岗位职责 + 申请基本条件 | 同文件：**第七条聘用程序**（设岗→公布→应聘→录用） |

观察：

- 两路 Top3 均落在正确规章文件中，未再出现明显跑题文档；
- Dense 已能稳定命中目标文件；
- Hybrid 将更直接回答“怎么申请”的**聘用程序**段落召入 Top3，但未排到 Top1；
- Top1 / Top2 多为“申请条件”或“岗位职责”，属于同主题次相关块。

**问句 B：研究生国家奖学金和国家助学金有什么区别？**

| 通道 | Top1 | Top2 / Top3 概况 |
|------|------|------------------|
| Dense | 国家奖助学金办法中对国家奖学金、学业奖学金、国家助学金用途的定义段落 | 同文件中的停发、申报与评审相关条款 |
| Hybrid | 与 Dense 相同的定义段落 | 同文件评审原则、停发与申报相关条款 |

观察：

- Dense 与 Hybrid 均能召回直接解释两者差异的定义段落；
- 该问句下两者差异较小，说明在语义清晰、专名完整的规章问题上，纯向量检索已经较强。

### 4. 结论

1. **工程可行性已验证。** 在不魔改 Chroma 的前提下，可使用 `rank_bm25` + RRF 稳定接入混合检索，并已接入对话链路 `retrieve`。
2. **中文分词是 BM25 通道的前提条件。** 正则整段切词会使关键字检索失效；jieba 与领域词表修正后，Hybrid 不再出现明显跨主题噪声。
3. **在本批验收问句上，Dense 基线已经较强。** Hybrid 的主要收益体现为稳住目标文档、并把部分更贴题段落纳入 Top3，而不是全面、稳定地超过 Dense。
4. **当前主要瓶颈从“召不回”转为“排不准”。** 以“怎么申请”为例，聘用程序块已被召回，但排序仍低于申请条件 / 职责说明块。这说明初筛融合后仍需要更精细的相关性重排。

### 5. 对项目的影响

相关能力已落地：

- `app/rag/hybrid.py`：Dense + BM25 + RRF；
- `app/rag/chain.py`：`retrieve` 初版接入 `hybrid_search`（后续由实验三叠加 Rerank）；
- `scripts/retrieve_probe.py`：Dense / Hybrid 对照探针；
- 依赖：`rank_bm25`、`jieba`。

### 6. 实验局限

1. 验收问句数量有限，结论不能外推为全库所有问题类型的统一提升；
2. 评估以人工阅读 Top3 预览为主，尚未计算 Hit@3、MRR 或 Ragas 指标；
3. RRF 只融合排名，不能区分“同文件次相关块”与“直接回答块”；
4. 切块边界仍会导致职责说明与申请条件出现在同一预览片段中，增加人工判断难度；
5. 本实验阶段尚未引入跨编码器 Rerank；“正确块已进入候选但排序靠后”的问题交给实验三验证。

因此，本部分结论表述为：混合检索双通道已可用，并改善了关键字通道稳定性；在当前问句集上，排序精度仍有提升空间，适宜作为后续 Rerank 实验的前置条件，而不表述为检索效果的最终最优方案。

---

## 实验三：Rerank 二次精排

### 1. 实验目标

实验二表明：在专名清晰的规章问句上，Dense / Hybrid 已能稳住目标文档，主要瓶颈从“召不回”转为“排不准”。典型例子是“研究生三助一辅岗位怎么申请？”——更直接回答流程的「第七条聘用程序」已进入 Hybrid Top3，但常排在「申请条件 / 岗位职责」之后。

RRF 只融合两路名次，不直接建模「当前问题与某段文字是否最贴意图」。本实验在 Hybrid 初筛结果之上引入跨编码器式 Rerank，对候选做二次相关性打分，验证能否改善进入生成阶段的 Top3 排序。

实验主要回答三个问题：

1. 能否在现有 Hybrid 链路外稳定接入云端 Rerank API？
2. 对 Hybrid Top10 做精排后，金标相关块在 Top3 中的位次是否改善？
3. Rerank 解决的是什么问题、不能解决什么问题？

### 2. 实验设置

| 项目 | 设置 |
|------|------|
| 初筛 | `hybrid_search`，`n_results=10`（Dense + BM25 + RRF） |
| 精排模型 | `BAAI/bge-reranker-v2-m3`（硅基流动 `/v1/rerank`） |
| 精排输入 | Hybrid 返回的 Top10 正文列表 |
| 精排输出 | 重排后 Top3 正文 |
| 实现文件 | `app/rag/rerank.py` |
| 链路接入 | `app/rag/chain.py` 的 `retrieve`：`hybrid_search(10)` → `rerank_documents(..., 3)` |
| 对照脚本 | `scripts/retrieve_probe.py`（`probe_rerank` / `compare_hybrid_vs_rerank`） |
| 评估方式 | 人工阅读预览 + 金标关键词位次（`hit_rank`） |

**方法说明：**

```text
问题
  └─ Hybrid：Dense + BM25 + RRF → Top10 候选正文
        └─ Rerank：query × 各候选成对打分 → 按相关性重排 → Top3
              └─ join 为 context → Prompt → LLM
```

说明：

- Rerank **不重新检索全库**，只对初筛池内候选重排；池中没有的块无法被“捞回”。
- API 返回的是候选下标 `index`（本实验设置 `return_documents=False`），再映射回原文列表。
- 候选为空直接返回；仅 1 条候选时跳过 API，原样返回。

**探针问句与金标关键词（示例）：**

| 问句 | 金标关键词 |
|------|------------|
| 本科生勤工助学怎么申请？ | 勤工助学 |
| 普通全日制本科生转专业需要什么条件？ | 转专业 |
| 新生可以申请保留入学资格吗？期限多久？ | 保留入学资格 |
| 缓考怎么申请？ | 缓考 |
| 教学事故怎么认定？ | 教学事故 |

位次判定：`hit_rank` 检查 Top3 中是否存在同时包含全部金标词的块；若有则返回其名次（1/2/3），否则为未命中。

### 3. 结果

#### 3.1 工程接入

已验证以下链路可运行：

1. Hybrid 产出 Top10 字符串列表；
2. 调用硅基流动 Rerank 接口完成重排；
3. `retrieve` 将精排后的 Top3 拼接为生成上下文；
4. 探针可同时打印 Hybrid Top10 预览与 Rerank Top3，并输出 Hybrid Top3 / Rerank Top3 的金标位次。

#### 3.2 Hybrid Top3 与 Rerank Top3 对照观察

对照逻辑与实验二一致：同一问句下比较「仅取 Hybrid 前三」与「Hybrid Top10 再 Rerank 取三」。

主要观察：

1. **Rerank 针对的是实验二暴露的排序问题。**  
   当目标流程/条件段落已进入 Hybrid 候选池时，精排有机会把更贴「怎么申请 / 怎么认定」意图的块前移，压低同文件中的职责说明、背景介绍等次相关块。

2. **对本来 Top1 已正确的问句，收益可能不明显。**  
   语义清晰、专名完整时，Hybrid（甚至 Dense）Top1 往往已经可用，Rerank 更多表现为顺序微调或基本不变。

3. **金标关键词过粗会削弱定量对比的灵敏度。**  
   例如仅用「勤工助学」「转专业」时，多段同主题块都可能同时命中，位次数字难以区分“次相关”与“直接回答”。人工阅读预览仍是必要补充。

4. **Rerank 不能弥补初筛漏召回。**  
   若正确块未进入 Hybrid Top10，精排无法从全库补回；此时应回查分词、切块或扩大候选数，而不是只调 Rerank。

#### 3.3 与实验二结论的衔接

| 阶段 | 主要作用 | 仍存缺口 |
|------|----------|----------|
| Dense / Hybrid | 稳住目标规章、降低跑题噪声 | 同主题次相关块可能压过直接答问块 |
| Rerank | 在 Top10 内按问题—段落相关性精排 | 依赖初筛质量；块边界混杂时仍难完美 |

### 4. 结论

1. **工程可行性已验证。** 在不改动 Chroma 内核的前提下，可用云端 `bge-reranker-v2-m3` 对 Hybrid 候选做二次精排，并已接入生产 `retrieve`。
2. **职责边界清晰。** Hybrid 负责召回与粗排；Rerank 负责候选内精排，不扩大召回范围。
3. **问题定位准确。** 本实验直接回应实验二“聘用程序在 Top3 但不是 Top1”类现象：相关块已在池中时，应用更强的 query—document 相关性模型，而不是宣称 Hybrid 全面优于 Dense。
4. **效果表述需克制。** 当前评估仍以探针预览与粗粒度金标位次为主，尚未用大规模 Ground Truth 或 Ragas 证明全面、稳定的指标提升。

### 5. 对项目的影响

相关能力已落地：

- `app/rag/rerank.py`：硅基流动 Rerank 封装；
- `app/rag/chain.py`：`retrieve = hybrid_search(10) → rerank_documents(top_n=3)`；
- `scripts/retrieve_probe.py`：增加 Rerank 预览与 Hybrid / Rerank 金标位次对照；
- 依赖：`httpx`（调用 `/v1/rerank`）。

生产检索链路现为：

```text
问题 → Hybrid Top10 → Rerank Top3 → Prompt → DeepSeek
```

### 6. 实验局限

1. 探针问句数量有限，金标多为单关键词，不能替代正式 Hit@3 / MRR；
2. 未计算 Context Precision、Faithfulness、Answer Relevancy 等生成侧指标；
3. Rerank 增加一次外部 API 调用，带来延迟与可用性依赖（密钥、网络、超时）；
4. 切块边界仍可能导致职责与条件同块混杂，精排只能缓解排序，不能拆开语义；
5. 未系统对比「无 Rerank（Hybrid Top3）」与「有 Rerank」在完整问答正确率上的差异。

因此，本部分结论表述为：Rerank 二次精排已接入，并针对实验二的“排不准”问题提供了工程解法；在当前探针评估下，它是对 Hybrid 粗排的必要补充，但尚不能表述为已用严格量化指标证明全面优于无 Rerank 基线。后续应用自建 Ground Truth 与 Ragas，对有无 Rerank 两组做定量对照。
